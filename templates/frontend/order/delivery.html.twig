{% extends 'base.html.twig' %}

{% block title %}Informations livraison | 3DWWM{% endblock %}

{% block content %}
    <h1>INFORMATIONS LIVRAISON</h1>

    <div class="row">
        <div class="col-md-6">
            {% set formHtml %}
                {{ form_start(form, {action:path('app_frontend_order_add')}) }}
                    {{ form_label(form.addresses, 'Adresse de livraison :') }}
                    <a href="{{ path('app_frontend_account_address_add') }}">Ajouter une adresse</a>
                {{ form_end(form) }}
            {% endset %}

            {{ formHtml|replace({'[br]' : '<br/>'})|raw }}
        </div>
        <div class="col-md-6">
            <p>Détail de ma commande :</p>
            {% for element in cart %}
                <div class="row">
                    <div class="col-8">
                        {{ element.product.name }}
                    </div>
                    <div class="col-4">
                        x{{ element.quantity }}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
